import { atom } from "recoil";

export const navModalOpen = atom({
    key: 'navModalOpen',
    default: false
});

export const admNavModalOpen = atom({
    key: 'admNavModalOpen',
    default: false
});